# 내비온 견적서 생성기 의존성
# 표준 설치: pip install -r requirements.txt
pywebview>=5.0      # 데스크톱 창 (Windows: WebView2 백엔드)
pyhwpx>=0.44        # 한글(HWP) COM 자동화 + 보안모듈 자동등록
requests>=2.30      # Gemini REST 호출
openpyxl>=3.1       # (참고용) 엑셀 모델 분석
olefile>=0.46       # HWP 바이너리 스캔 (PrvText/BodyText)
pypdfium2>=4        # 스캔 PDF → 이미지 렌더 (LLM 비전 전사 폴백)
Pillow>=10          # pypdfium2 렌더 결과 PNG 인코딩
pywin32>=305        # COM, DPAPI(win32crypt) 키 암호화
# Google Drive 연동 (선택 — 미설치 시 Drive 기능만 비활성)
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
# 테스트: pytest>=8
# 회의 녹음 전사(STT) — 선택 설치. 미설치 시 STT 기능만 비활성(앱 부팅·나머지 기능 정상)
# 근거: docs/PRD_회의록STT.md v0.2 §7.6 (faster-whisper=MIT, sherpa-onnx=Apache-2.0, 둘 다 PyTorch 불필요)
faster-whisper>=1.0    # 전사(CTranslate2, CPU int8). PyAV 내장 → ffmpeg.exe 별도 설치 불필요
sherpa-onnx>=1.10      # 화자 분리(ONNX Runtime). 모델은 GitHub Releases에서 최초 실행 시 다운로드
